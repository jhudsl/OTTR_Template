
# Jan 2022 Candace Savonen

name: Render and publish Leanpub

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]
### UNCOMMENT THIS SECTION IF YOU WISH PREPARE YOUR COURSE FOR LEANPUB AUTOMATICALLY
#  pull_request:
#    branches: [ main ]
#    types: [closed]
#    paths:
#      - '**.Rmd'
#      - docker/*
#      - assets/*
#      - quizzes/*
######### END OF SECTION #########

jobs:
  # This workflow contains a single job called "build-all"
  render-main:
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: checkout
        uses: actions/checkout@v2
        with:
          # get the full repo
          fetch-depth: 0
          # use github PAT
          token: ${{ secrets.GH_PAT }}

      # Run leanpub rendering
      - name: Install packages we need
        run: |
          Rscript -e "remotes::install_github("jhudsl/cow", auth_token = ${{ secrets.GH_PAT }})"
          Rscript -e "install.packages(optparse)"

      # Run leanpub rendering
#      - name: Run leanbuild::bookdown_to_leanpub
#        run: Rscript -e "leanbuild::bookdown_to_leanpub(make_book_txt = TRUE)"

      # Convert quizzes to Coursera format
#      - name: Convert quizzes to coursera
#        run: Rscript -e "leanbuild::render_coursera(convert_quizzes = TRUE)"
      - name: Create pngs of chapters
        id: chapt_urls
        run: |
          Rscript --vanilla scripts/get_chapter_urls.R \
            --git_pat ${{ secrets.GH_PAT }} \
            --repo $GITHUB_REPOSITORY)

      # Commit the rendered leanpub files
    #  - name: Commit rendered leanpub files
        #run: |
        #  git config --local user.email "actions@github.com"
        #  git config --local user.name "GitHub Actions"
        #  git add -A
        #  git add --force manuscript/*
        #  git commit -m 'Render leanpub' || echo "No changes to commit"
        #  git push origin main || echo "No changes to push"
